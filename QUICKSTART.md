# 🚀 Quick Start Guide - Mini Chat Frontend

คู่มือเริ่มต้นใช้งานอย่างรวดเร็วสำหรับ Mini Chat Frontend

## ⚡ เริ่มต้นใน 3 ขั้นตอน

### ขั้นตอนที่ 1: เริ่ม Backend

```bash
cd ../mini-chat-backend
docker-compose -f docker-compose.full.yml up -d
```

รอ 10-15 วินาที แล้วตรวจสอบ:
```bash
curl http://localhost:4001/api/users
```

### ขั้นตอนที่ 2: Setup Frontend

```bash
cd ../mini-chat-frontend

# ติดตั้ง dependencies
npm install

# สร้างไฟล์ .env.local
echo "NEXT_PUBLIC_API_BASE_URL=http://localhost:4001" > .env.local
echo "NEXT_PUBLIC_POLLING_INTERVAL=2000" >> .env.local
```

### ขั้นตอนที่ 3: เริ่ม Frontend

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ `http://localhost:3000`

## ✅ เสร็จสิ้น!

---

## 🎮 วิธีใช้งาน Demo

### 1. Switch User (สลับผู้ใช้)

**ที่มุมบนขวา** จะมี dropdown แสดง:
```
Switch user: [Alice ▼]
```

คลิกเพื่อเลือกผู้ใช้:
- Alice (ID: 1)
- Bob (ID: 2)
- Charlie (ID: 3)
- Dave (ID: 4)

### 2. สร้าง Conversation

**ด้านซ้าย** มี dropdown "Start chat with…":

1. เลือกผู้ใช้ที่ต้องการแชทด้วย
2. คลิกปุ่ม **"New"** (สีน้ำเงิน-ม่วง)
3. Conversation ใหม่จะปรากฏในรายการ

### 3. ส่งข้อความ

1. คลิกที่ conversation ในรายการด้านซ้าย
2. พิมพ์ข้อความในช่อง input ด้านล่าง
3. กด **Enter** หรือคลิก **Send**

### 4. ทดสอบ Real-time

**วิธีที่ 1: ใช้ 1 เบราว์เซอร์**
```
1. Login เป็น Alice
2. สร้าง conversation กับ Bob
3. ส่งข้อความ "สวัสดี!"
4. Switch user เป็น Bob (มุมบนขวา)
5. เปิด conversation กับ Alice
6. เห็นข้อความจาก Alice
7. ตอบกลับ "สวัสดีครับ!"
```

**วิธีที่ 2: ใช้ 2 หน้าต่าง (แนะนำ)**
```
หน้าต่างที่ 1 (Normal):
- เปิด http://localhost:3000
- Login เป็น Alice

หน้าต่างที่ 2 (Incognito/Private):
- เปิด http://localhost:3000
- Login เป็น Bob

จากนั้นแชทกันระหว่าง 2 หน้าต่าง!
```

---

## 🎨 Features ที่น่าสนใจ

### 1. Live Updates
- ข้อความอัปเดตทุก 2 วินาที
- แสดงสถานะ "Live" เมื่อ polling ทำงาน
- หยุด polling ชั่วคราวเมื่อกำลังพิมพ์

### 2. Smart Scroll
- Auto scroll ลงล่างเมื่อมีข้อความใหม่
- คงตำแหน่งเมื่อโหลดข้อความเก่า
- Smooth scroll animation

### 3. Beautiful UI
- Gradient สีสวยงาม
- Avatar แสดงตัวอักษรแรก
- Message bubbles แบบ modern
- Dark mode support

### 4. Responsive
- ใช้งานได้ดีบนมือถือ
- ปรับ layout ตามขนาดหน้าจอ
- Custom scrollbar

---

## 🔧 การปรับแต่ง

### เปลี่ยน Polling Speed

แก้ไขไฟล์ `.env.local`:

```env
# เร็วขึ้น (1 วินาที)
NEXT_PUBLIC_POLLING_INTERVAL=1000

# ช้าลง (5 วินาที)
NEXT_PUBLIC_POLLING_INTERVAL=5000
```

**อย่าลืม restart dev server!**

### เปลี่ยน Backend URL

```env
# Production
NEXT_PUBLIC_API_BASE_URL=https://api.example.com

# Local
NEXT_PUBLIC_API_BASE_URL=http://localhost:4001
```

---

## 🐛 แก้ปัญหาเบื้องต้น

### ปัญหา: ไม่เห็นข้อความอัปเดต

**แก้:**
1. ตรวจสอบ Backend ทำงานหรือไม่: `curl http://localhost:4001/api/users`
2. ดู Console (F12) ว่ามี error หรือไม่
3. Restart dev server

### ปัญหา: Environment variables ไม่ทำงาน

**แก้:**
1. ตรวจสอบชื่อไฟล์: `.env.local` (ไม่ใช่ `.env`)
2. ตรวจสอบว่าขึ้นต้นด้วย `NEXT_PUBLIC_`
3. **Restart dev server** (สำคัญ!)

### ปัญหา: Port 3000 ถูกใช้แล้ว

**แก้:**
```bash
# ใช้ port อื่น
npm run dev -- -p 3001
```

---

## 📝 Checklist

- [ ] Backend ทำงานอยู่ (http://localhost:4001)
- [ ] ติดตั้ง dependencies แล้ว (`npm install`)
- [ ] สร้างไฟล์ `.env.local` แล้ว
- [ ] เริ่ม dev server แล้ว (`npm run dev`)
- [ ] เปิดเบราว์เซอร์ที่ http://localhost:3000
- [ ] ทดสอบ switch user
- [ ] ทดสอบสร้าง conversation
- [ ] ทดสอบส่งข้อความ

**ถ้าทุกอย่างทำงาน คุณพร้อมใช้งานแล้ว! 🎉**

---

## 📚 เอกสารเพิ่มเติม

- [README.md](./README.md) - เอกสารฉบับเต็ม
- [Backend README](../mini-chat-backend/README.md) - เอกสาร Backend

---

**Happy Chatting! 💬**
